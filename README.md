# Reproduction for Starcounter/Level1#4755

1. Build and run both applications
1. Open `/StarcounterApplication7/Legacy` in a browser
1. Note that blended view-models report different values of `AttachedScope`
